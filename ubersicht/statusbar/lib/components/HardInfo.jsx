import { styled } from "uebersicht";
import { useProcess } from "../utils";

export const HardInfo = () => {
  const mem = useProcess("");
  return <Wrapper></Wrapper>;
};

const Wrapper = styled.div``;
